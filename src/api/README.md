This folder is reserved for future API clients and data adapters.

The current UI foundation uses static data from `src/data` so backend, quiz,
and translation modules can be integrated later without changing page structure.
